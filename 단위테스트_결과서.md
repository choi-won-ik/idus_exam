# DocMind AI - 단위테스트 결과서

---

## 문서 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | DocMind AI - 생성형 AI기반 문서요약시스템 |
| 테스트 유형 | 단위테스트 (Unit Test) |
| 작성일 | 2026-05-23 |
| 테스트 환경 | Python 3.11 / pytest / pytest-mock / FastAPI TestClient / SQLite In-Memory DB |
| 테스트 대상 | core/security.py, services/auth_service.py, api/routes/auth.py, db/document.py, api/routes/documents.py, services/document_service.py, services/summary_service.py, services/pdf_service.py, services/hwp_service.py, services/docx_service.py |

---

## 1. 보안 모듈 (core/security.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-SEC-001 | `hash_password(password)` | 보안 | 비밀번호 해싱 | 평문 비밀번호를 sha256_crypt로 해싱 후 평문과 다른 문자열 반환 확인 | `password = "Test1234!"` | 1. `hash_password("Test1234!")` 호출<br>2. 반환값이 평문과 다른지 확인<br>3. 반환값이 문자열 타입인지 확인 | 반환값 != `"Test1234!"`, 문자열 타입, sha256 해시 접두사(`$5$`) 포함 | 반환값: `"$5$rounds=535000$…"` (sha256_crypt 해시), 평문과 다름, str 타입 확인 | PASS | - | `passwordd_context`(오타) 동작에는 이상 없음 |
| TC-SEC-002 | `hash_password(password)` | 보안 | 비밀번호 해싱 | 동일 평문으로 두 번 해싱 시 결과값이 다른지(Salt 적용) 확인 | `password = "SamePass99"` | 1. `hash_password("SamePass99")` 두 번 호출<br>2. 두 반환값 비교 | 두 해시값이 서로 다름 (Salt 기반 랜덤성) | hash1 != hash2 확인 (`$5$` 접두사 공통, Salt 부분 상이) | PASS | - | Rainbow Table 공격 방어 확인 |
| TC-SEC-003 | `verify_password(plain, hashed)` | 보안 | 비밀번호 검증 | 올바른 평문과 해시 쌍 검증 시 True 반환 | `plain="Test1234!"`, `hashed=hash_password("Test1234!")` | 1. `hash_password("Test1234!")` 로 해시 생성<br>2. `verify_password("Test1234!", hashed)` 호출<br>3. 반환값 확인 | `True` 반환 | `True` 반환 확인 | PASS | - | - |
| TC-SEC-004 | `verify_password(plain, hashed)` | 보안 | 비밀번호 검증 | 틀린 평문과 해시 쌍 검증 시 False 반환 | `plain="WrongPass!"`, `hashed=hash_password("Test1234!")` | 1. `verify_password("WrongPass!", hashed)` 호출<br>2. 반환값 확인 | `False` 반환 | `False` 반환 확인 | PASS | - | - |
| TC-SEC-005 | `create_access_token(data)` | 보안 | Access Token 생성 | 정상 페이로드로 Access Token 생성 후 구조 및 만료시간 검증 | `data = {"sub": "1"}`, `expires_delta = timedelta(minutes=30)` | 1. `create_access_token({"sub": "1"})` 호출<br>2. 반환 JWT 디코딩(검증)<br>3. `type`, `sub`, `exp` 클레임 확인 | JWT 문자열 반환, decode 후 `sub="1"`, `type="access"`, `exp`=현재시각+30분 | JWT 문자열 반환, `sub="1"`, `type="access"`, `exp` 만료 시각 정상 계산됨 | PASS | - | HS256 알고리즘 사용 |
| TC-SEC-006 | `create_refresh_token(data)` | 보안 | Refresh Token 생성 | 정상 페이로드로 Refresh Token 생성 후 만료 7일 확인 | `data = {"sub": "1"}` | 1. `create_refresh_token({"sub": "1"})` 호출<br>2. JWT 디코딩 후 `type`, `exp` 확인 | `type="refresh"`, `exp`=현재시각+7일 | `type="refresh"`, `exp` 7일 만료 시각 정상 설정 확인 | PASS | - | - |
| TC-SEC-007 | `verify_token(token, token_type)` | 보안 | 토큰 검증 | 유효한 Access Token 검증 시 페이로드 반환 | `token=create_access_token({"sub":"1"})`, `token_type="access"` | 1. `verify_token(token, "access")` 호출<br>2. 반환 페이로드의 `sub` 값 확인 | `{"sub": "1", "type": "access", "exp": ...}` dict 반환 | `{"sub": "1", "type": "access", "exp": <timestamp>}` 정상 반환 | PASS | - | - |
| TC-SEC-008 | `verify_token(token, token_type)` | 보안 | 토큰 타입 불일치 검증 | Access Token으로 refresh 타입 검증 시도 시 예외 발생 | `token=create_access_token({"sub":"1"})`, `token_type="refresh"` | 1. `verify_token(token, "refresh")` 호출<br>2. 예외 타입 및 상태코드 확인 | `HTTPException(401, "토큰이 유효하지 않습니다")` 발생 | `HTTPException` status_code=401, detail="토큰이 유효하지 않습니다" 발생 확인 | PASS | - | type 불일치 차단 확인 |
| TC-SEC-009 | `verify_token(token, token_type)` | 보안 | 위변조 토큰 검증 | 서명이 잘못된 JWT 전달 시 예외 발생 | `token="eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiIxIn0.INVALIDSIG"` | 1. 변조된 JWT로 `verify_token` 호출<br>2. JWTError 캐치 후 HTTPException 발생 확인 | `HTTPException(401)` 발생 | `JWTError` → `HTTPException(401)` 정상 발생 | PASS | - | `JWTError` → 401 변환 확인 |
| TC-SEC-010 | `get_current_active_user` | 보안 | 비활성 계정 차단 | `is_active=False` 사용자 접근 시 403 예외 발생 | `user.is_active = False` | 1. `is_active=False` User Mock 생성<br>2. `get_current_active_user(current_user=mock_user)` 호출<br>3. 예외 확인 | `HTTPException(403, "비활성화된 계정입니다")` 발생 | `HTTPException` status_code=403, detail="비활성화된 계정입니다" 발생 | PASS | - | - |

---

## 2. 인증 서비스 (services/auth_service.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-ASVC-001 | `get_user(db, id)` | 인증 서비스 | ID 기반 사용자 조회 | 존재하는 ID로 사용자 조회 시 User 객체 반환 | `id=1`, DB에 사용자 존재 | 1. In-Memory DB에 User 레코드 삽입<br>2. `get_user(db, 1)` 호출<br>3. 반환 객체 필드 확인 | `User(id=1, email="test@test.com", name="홍길동")` 반환 | `User` 객체 반환, id=1, email, name 필드 일치 확인 | PASS | - | SQLAlchemy `.first()` 동작 확인 |
| TC-ASVC-002 | `get_user(db, id)` | 인증 서비스 | ID 기반 사용자 조회 | 존재하지 않는 ID 조회 시 None 반환 | `id=9999` (미존재) | 1. `get_user(db, 9999)` 호출<br>2. 반환값 확인 | `None` 반환 | `None` 반환 확인 | PASS | - | - |
| TC-ASVC-003 | `get_user_by_email(db, email)` | 인증 서비스 | 이메일 기반 사용자 조회 | 등록된 이메일로 조회 시 User 객체 반환 | `email="test@test.com"` | 1. DB에 해당 이메일 User 삽입<br>2. `get_user_by_email(db, "test@test.com")` 호출<br>3. 반환 객체 확인 | `User` 객체 반환, `email="test@test.com"` | `User` 객체 정상 반환, email 일치 확인 | PASS | - | - |
| TC-ASVC-004 | `get_user_by_email(db, email)` | 인증 서비스 | 이메일 기반 사용자 조회 | 미등록 이메일로 조회 시 None 반환 | `email="none@none.com"` | 1. `get_user_by_email(db, "none@none.com")` 호출<br>2. 반환값 확인 | `None` 반환 | `None` 반환 확인 | PASS | - | - |
| TC-ASVC-005 | `create_user(db, user)` | 인증 서비스 | 사용자 생성 | 유효한 UserCreate 스키마로 사용자 생성 후 DB 저장 확인 | `UserCreate(email="new@test.com", password="Abc123!", name="김철수")` | 1. `create_user(db, user_schema)` 호출<br>2. 반환 User 객체 확인<br>3. `password` 필드가 해시값인지 확인<br>4. DB에서 직접 조회하여 영속 확인 | `User` 반환, `password`가 해시값(`$5$...`), DB에 레코드 존재 | `User` 반환, password 필드 `$5$` 해시 확인, DB 레코드 생성 확인 | PASS | - | `hash_password` 연동 확인 |
| TC-ASVC-006 | `delete_user(db, id)` | 인증 서비스 | 사용자 삭제 | 존재하는 사용자 삭제 후 DB에서 제거 확인 | `id=1` (존재하는 사용자) | 1. `delete_user(db, 1)` 호출<br>2. 반환 User 객체 확인<br>3. `get_user(db, 1)` 재조회 시 None 반환 확인 | 삭제된 `User` 객체 반환, 재조회 시 `None` | `User` 반환 후 `get_user(db, 1)` → `None` 확인 | PASS | - | Hard delete 동작 확인 |
| TC-ASVC-007 | `delete_user(db, id)` | 인증 서비스 | 사용자 삭제 | 존재하지 않는 ID 삭제 시도 시 None 반환 | `id=9999` | 1. `delete_user(db, 9999)` 호출<br>2. 반환값 확인 | `None` 반환, DB 변경 없음 | `None` 반환 확인, DB 상태 유지 확인 | PASS | - | - |

---

## 3. 인증 라우터 (api/routes/auth.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-AUTH-001 | `POST /auth/register` | 인증 라우터 | 회원가입 | 유효한 데이터로 회원가입 성공 | `{"email":"reg@test.com","password":"Pass1!","name":"이순신"}` | 1. TestClient로 `POST /auth/register` 호출<br>2. 응답 status_code 확인<br>3. 응답 Body의 id, email, name 확인<br>4. password 필드 미포함 확인 | HTTP 200, `{"id":..,"email":"reg@test.com","name":"이순신"}`, password 미포함 | HTTP 200, 응답 Body에 id, email, name 정상 포함, password 미반환 확인 | PASS | - | `response_model=UserResponse` 적용 확인 |
| TC-AUTH-002 | `POST /auth/register` | 인증 라우터 | 중복 이메일 방지 | 이미 등록된 이메일로 재가입 시 400 반환 | `email="reg@test.com"` (기등록) | 1. 동일 이메일로 `POST /auth/register` 재호출<br>2. status_code, detail 확인 | HTTP 400, `{"detail":"이미 존재하는 이메일입니다"}` | HTTP 400, detail="이미 존재하는 이메일입니다" 반환 | PASS | - | `get_user_by_email` 중복 체크 경로 확인 |
| TC-AUTH-003 | `POST /auth/login` | 인증 라우터 | 로그인 성공 | 등록 계정으로 로그인 후 JWT 쿠키 발급 확인 | `{"email":"reg@test.com","password":"Pass1!"}` | 1. `POST /auth/login` 호출<br>2. 응답 Set-Cookie 헤더 확인<br>3. `access_token`, `refresh_token` 쿠키 확인<br>4. httponly, samesite=lax 속성 확인 | HTTP 200, `{"message":"로그인 성공"}`, access_token(max_age=1800), refresh_token(max_age=604800) HttpOnly 쿠키 발급 | HTTP 200, 메시지 정상, access_token/refresh_token 쿠키 2종 발급, max_age 일치, HttpOnly 속성 확인 | PASS | - | `secure=False` (개발 환경) |
| TC-AUTH-004 | `POST /auth/login` | 인증 라우터 | 로그인 실패 - 틀린 비밀번호 | 잘못된 비밀번호 입력 시 401 반환 | `{"email":"reg@test.com","password":"WrongPass!"}` | 1. `POST /auth/login` 호출<br>2. status_code, detail 확인 | HTTP 401, `{"detail":"이메일 또는 비밀번호가 올바르지 않습니다"}` | HTTP 401, detail 메시지 일치 확인, 쿠키 미발급 확인 | PASS | - | `verify_password` 실패 경로 |
| TC-AUTH-005 | `POST /auth/login` | 인증 라우터 | 로그인 실패 - 미등록 이메일 | 존재하지 않는 이메일로 로그인 시 401 반환 | `{"email":"none@none.com","password":"Any1234!"}` | 1. `POST /auth/login` 호출<br>2. status_code 확인 | HTTP 401, `{"detail":"이메일 또는 비밀번호가 올바르지 않습니다"}` | HTTP 401, detail 메시지 일치, `get_user_by_email` → None 분기 확인 | PASS | - | 존재/비밀번호 오류 메시지 동일 (보안상) |
| TC-AUTH-006 | `GET /auth/me` | 인증 라우터 | 내 정보 조회 - 유효 토큰 | 유효한 access_token 쿠키로 사용자 정보 조회 | 유효한 `access_token` 쿠키 | 1. 로그인 후 발급된 쿠키 포함하여 `GET /auth/me` 호출<br>2. 응답 확인 | HTTP 200, `{"authenticated":True,"id":1,"email":"reg@test.com","name":"이순신"}` | HTTP 200, authenticated=True, 사용자 정보 정상 반환 확인 | PASS | - | - |
| TC-AUTH-007 | `GET /auth/me` | 인증 라우터 | 내 정보 조회 - 만료 후 refresh 재발급 | access_token 만료 시 refresh_token으로 자동 갱신 후 응답 | 만료된 `access_token`, 유효한 `refresh_token` | 1. access_token 만료 처리(mock)<br>2. refresh_token 쿠키 포함하여 `GET /auth/me` 호출<br>3. 응답 쿠키에 새 access_token 확인 | HTTP 200, `authenticated=True`, Set-Cookie에 신규 access_token(max_age=1800) 발급 | HTTP 200, authenticated=True, 응답 Set-Cookie에 새 access_token 쿠키 확인 | PASS | - | JWTError → refresh 분기 동작 확인 |
| TC-AUTH-008 | `GET /auth/me` | 인증 라우터 | 내 정보 조회 - 모든 토큰 없음 | 쿠키 없이 요청 시 인증 실패 응답 | 쿠키 없음 | 1. 쿠키 없이 `GET /auth/me` 호출<br>2. 응답 확인 | HTTP 200, `{"authenticated": False}` | HTTP 200, `{"authenticated": False}` 반환 (예외 아닌 soft 처리) | PASS | - | 401이 아닌 200+False 반환 설계 확인 |
| TC-AUTH-009 | `POST /auth/logout` | 인증 라우터 | 로그아웃 | 로그아웃 후 쿠키 삭제 확인 | 로그인 상태의 쿠키 보유 | 1. `POST /auth/logout` 호출<br>2. 응답 Set-Cookie 헤더 확인(max-age=0 또는 delete_cookie) | HTTP 200, `{"message":"로그아웃 성공"}`, `access_token`/`refresh_token` 쿠키 삭제 | HTTP 200, 메시지 정상, 응답 헤더에 쿠키 삭제 지시 확인 | PASS | - | - |
| TC-AUTH-010 | `POST /auth/refresh` | 인증 라우터 | 토큰 갱신 | 유효한 refresh_token으로 새 access/refresh 토큰 발급 | `{"refresh_token": "<valid_jwt>"}` | 1. `POST /auth/refresh` Body에 refresh_token 포함 호출<br>2. 응답의 access_token, refresh_token 확인 | HTTP 200, `Token(access_token=<new>, refresh_token=<new>)` | HTTP 200, 신규 access_token, refresh_token 정상 반환 | PASS | - | `response_model=Token` 적용 확인 |

---

## 4. 문서 레포지토리 (db/document.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-REPO-001 | `insert_document(db, owner_id, title, content, category)` | 문서 레포지토리 | 문서 저장 | 유효한 파라미터로 문서 생성 및 DB 저장 | `owner_id=1, title="기술 보고서", content="본문 텍스트", category="기술문서"` | 1. `insert_document(db, 1, "기술 보고서", "본문 텍스트", "기술문서")` 호출<br>2. 반환 Document 객체 확인<br>3. `id`, `is_deleted`, `created_at` 필드 확인 | `Document(id=1, owner_id=1, title="기술 보고서", category="기술문서", is_deleted=False)` 반환 | `Document` 객체 반환, is_deleted=False, created_at KST 현재 시각, id 자동 부여 확인 | PASS | - | auto-increment PK 확인 |
| TC-REPO-002 | `insert_document` | 문서 레포지토리 | 문서 저장 | summary 미포함 상태로 저장 후 summary=None 확인 | `owner_id=1, title="초안", content="내용", category="일반문서"` | 1. `insert_document` 호출 (summary 인자 없음)<br>2. 반환 객체의 `summary` 필드 확인 | `summary=None` | `summary=None` 확인 (별도 `update_document_summary` 호출로 저장) | PASS | - | 저장-요약 2단계 분리 구조 확인 |
| TC-REPO-003 | `update_document_summary(db, document_id, summary_content)` | 문서 레포지토리 | 요약 저장 | 문서에 요약 내용 업데이트 후 반영 확인 | `document_id=1, summary_content="• 핵심 항목1\n• 핵심 항목2"` | 1. 문서 삽입 후 `update_document_summary(db, 1, "• 핵심 항목1\n• 핵심 항목2")` 호출<br>2. 반환 객체 summary 필드 확인<br>3. DB에서 재조회하여 영속 확인 | `Document.summary="• 핵심 항목1\n• 핵심 항목2"` | summary 필드 정상 업데이트, DB 재조회 시 동일값 확인 | PASS | - | - |
| TC-REPO-004 | `update_document_summary(db, document_id, summary_content)` | 문서 레포지토리 | 요약 저장 | 미존재 document_id로 요약 저장 시 None 반환 | `document_id=9999` | 1. `update_document_summary(db, 9999, "요약")` 호출<br>2. 반환값 확인 | `None` 반환 | `None` 반환 확인 | PASS | - | - |
| TC-REPO-005 | `update_document(db, id, document_data)` | 문서 레포지토리 | 문서 수정 | title, category 수정 후 반영 확인 (`summary` 필드는 exclude) | `id=1, DocumentUpdate(title="수정 제목", category="일반문서")` | 1. 문서 삽입 후 `update_document(db, 1, DocumentUpdate(...))` 호출<br>2. title, category 변경 확인<br>3. summary가 변경 대상에서 제외됨 확인 | `Document(title="수정 제목", category="일반문서")` 반환, summary 필드 변경 없음 | title, category 수정 반영, summary 필드 기존값 유지 확인 (`exclude={"summary"}` 적용됨) | PASS | - | `exclude={"summary"}` 의도적 설계 확인 |
| TC-REPO-006 | `update_document(db, id, document_data)` | 문서 레포지토리 | 문서 수정 | 미존재 ID 수정 시 None 반환 | `id=9999` | 1. `update_document(db, 9999, DocumentUpdate(title="x"))` 호출<br>2. 반환값 확인 | `None` 반환 | `None` 반환 확인 | PASS | - | - |
| TC-REPO-007 | `update_delete_document(db, id)` | 문서 레포지토리 | 문서 소프트 삭제 | is_deleted 플래그를 True로 변경하는 소프트 삭제 | `id=1` | 1. 문서 삽입 후 `update_delete_document(db, 1)` 호출<br>2. 반환 객체의 `is_deleted` 확인<br>3. DB에서 재조회하여 `is_deleted=True` 확인 | `Document(is_deleted=True)` 반환, DB 레코드 is_deleted=1 | `is_deleted=True` 반환, DB 재조회 시 is_deleted=1 확인 (물리 삭제 아님) | PASS | - | - |
| TC-REPO-008 | `update_delete_document(db, id)` | 문서 레포지토리 | 문서 소프트 삭제 | 미존재 ID 삭제 시 None 반환 | `id=9999` | 1. `update_delete_document(db, 9999)` 호출<br>2. 반환값 확인 | `None` 반환 | `None` 반환 확인 | PASS | - | - |
| TC-REPO-009 | `sort_document(db, owner_id, sort)` | 문서 레포지토리 | 문서 정렬 | `sort="latest"` 옵션 시 created_at 내림차순 쿼리 반환 | `owner_id=1`, 문서 3건(created_at 상이), `sort="latest"` | 1. 문서 3건 삽입 (시간 상이)<br>2. `sort_document(db, 1, "latest")` 호출<br>3. `.all()` 결과의 순서 확인 | 가장 최근 문서가 첫 번째, created_at DESC 정렬 | created_at DESC 정렬 확인, is_deleted=False 필터 동작 확인 | PASS | - | - |
| TC-REPO-010 | `sort_document(db, owner_id, sort)` | 문서 레포지토리 | 문서 정렬 | `sort="name-asc"` 옵션 시 title 오름차순 | `owner_id=1`, `sort="name-asc"` | 1. `sort_document(db, 1, "name-asc")` 호출<br>2. 결과 title 순서 확인 | title ASC 정렬 | title 가나다 순 정렬 확인 | PASS | - | - |
| TC-REPO-011 | `sort_document(db, owner_id, sort)` | 문서 레포지토리 | 문서 정렬 | 미정의 sort 값 전달 시 `latest` 기본 정렬 적용 | `sort="invalid_sort"` | 1. `sort_document(db, 1, "invalid_sort")` 호출<br>2. 결과 순서 확인 | created_at DESC 기본 정렬 (else 분기) | created_at DESC 적용 확인 | PASS | - | else 분기 기본값 동작 확인 |
| TC-REPO-012 | `search_document(db, owner_id, query)` | 문서 레포지토리 | 문서 검색 | 제목 키워드 검색 시 ilike 쿼리로 결과 반환 | `owner_id=1`, `query="보고서"`, 해당 문서 2건 존재 | 1. 문서 3건 삽입(제목: "기술 보고서", "연간 보고서", "계획서")<br>2. `search_document(db, 1, "보고서")` 호출<br>3. 반환 쿼리 `.all()` 결과 확인 | "보고서" 포함 문서 2건 반환, "계획서" 미포함 | "기술 보고서", "연간 보고서" 2건 반환, "계획서" 제외 확인 (ilike 대소문자 무관) | PASS | - | `%보고서%` LIKE 쿼리 확인 |
| TC-REPO-013 | `search_document(db, owner_id, query)` | 문서 레포지토리 | 문서 검색 | 미존재 키워드 검색 시 빈 결과 반환 | `query="없는키워드xyz"` | 1. `search_document(db, 1, "없는키워드xyz")` 호출<br>2. `.all()` 결과 확인 | 빈 리스트 `[]` 반환 | `[]` 반환 확인 | PASS | - | - |

---

## 5. 문서 라우터 (api/routes/documents.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-DOC-001 | `get_current_user_id` (의존성) | 문서 라우터 | 인증 의존성 | 토큰 없이 문서 목록 요청 시 401 반환 | 쿠키: None | 1. 쿠키 없이 `GET /documents/list` 호출<br>2. status_code 확인 | HTTP 401, `{"detail":"인증 토큰이 없습니다."}` | HTTP 401, detail="인증 토큰이 없습니다." 반환 | PASS | - | `access_token is None` 분기 확인 |
| TC-DOC-002 | `get_current_user_id` (의존성) | 문서 라우터 | 인증 의존성 | 잘못된 토큰으로 요청 시 401 반환 | `access_token="invalid.jwt.token"` | 1. 변조된 토큰 쿠키로 `GET /documents/list` 호출<br>2. status_code 확인 | HTTP 401, `{"detail":"토큰 검증에 실패했습니다."}` | HTTP 401, detail="토큰 검증에 실패했습니다." 반환, JWTError 캐치 확인 | PASS | - | - |
| TC-DOC-003 | `GET /documents/list` | 문서 라우터 | 목록 조회 (페이지네이션) | 문서 7건 보유 시 page=1, size=5 요청 결과 | `owner_id=1`, 문서 7건, `page=1, size=5` | 1. 유효 토큰 쿠키와 `GET /documents/list?page=1&size=5` 호출<br>2. items 수, total, total_pages 확인 | `{"items":[...5건...],"total":7,"page":1,"size":5,"total_pages":2}` | items 5건, total=7, page=1, size=5, total_pages=2 정상 반환 | PASS | - | ceil 나눗셈 확인: (7+5-1)//5=2 |
| TC-DOC-004 | `GET /documents/list` | 문서 라우터 | 목록 조회 | is_deleted=True 문서 목록에서 제외 확인 | `owner_id=1`, 문서 3건 중 1건 is_deleted=True | 1. `GET /documents/list` 호출<br>2. 반환 items 수 확인 | items 2건 반환 (삭제 문서 제외) | items 2건 반환, 삭제 문서 미포함 확인 | PASS | - | `is_deleted == False` 필터 확인 |
| TC-DOC-005 | `GET /documents/{document_id}` | 문서 라우터 | 문서 상세 조회 | 본인 문서 ID로 상세 조회 | `document_id=1`, `owner_id=1` | 1. `GET /documents/1` 호출<br>2. 응답 필드 확인 | HTTP 200, `{"id":1,"title":...,"content":...,"category":...,"summary":...,"created_at":...}` | HTTP 200, 6개 필드 모두 포함, summary 포함 확인 | PASS | - | - |
| TC-DOC-006 | `GET /documents/{document_id}` | 문서 라우터 | 문서 상세 조회 | 타인 문서 ID 접근 시 404 반환 | `document_id=2` (타 사용자 소유) | 1. owner_id=1 토큰으로 `GET /documents/2` 호출<br>2. 응답 확인 | HTTP 404, `{"detail":"문서를 찾을 수 없습니다."}` | HTTP 404, detail 메시지 일치 (owner_id 필터로 자동 차단) | PASS | - | owner_id 기반 접근 제어 확인 |
| TC-DOC-007 | `GET /documents/{document_id}` | 문서 라우터 | 문서 상세 조회 | 삭제된 문서 ID 접근 시 404 반환 | `document_id=1` (is_deleted=True) | 1. `GET /documents/1` 호출 (해당 문서 삭제 상태)<br>2. 응답 확인 | HTTP 404, `{"detail":"문서를 찾을 수 없습니다."}` | HTTP 404 반환, is_deleted=True 문서 조회 차단 확인 | PASS | - | - |
| TC-DOC-008 | `PUT /documents/modify/{document_id}` | 문서 라우터 | 문서 수정 | title, category 수정 성공 | `document_id=1, DocumentUpdate(title="새 제목", category="일반문서")` | 1. `PUT /documents/modify/1` 호출 (Body: DocumentUpdate)<br>2. 응답 Document 객체 확인 | HTTP 200, 수정된 Document 반환, title="새 제목", category="일반문서" | HTTP 200, 반환 객체 title/category 변경 확인 | PASS | - | `update_document` 연동 확인 |
| TC-DOC-009 | `PUT /documents/modify/{document_id}` | 문서 라우터 | 문서 수정 | 미존재 ID 수정 시 404 반환 | `document_id=9999` | 1. `PUT /documents/modify/9999` 호출<br>2. 응답 확인 | HTTP 404, `{"detail":"수정에 실패하였습니다."}` | HTTP 404, detail 메시지 일치, `update_document` → None 분기 확인 | PASS | - | - |
| TC-DOC-010 | `DELETE /documents/delete/{document_id}` | 문서 라우터 | 문서 삭제 | 존재하는 문서 삭제 성공 | `document_id=1` | 1. `DELETE /documents/delete/1` 호출<br>2. 응답 확인<br>3. `GET /documents/1` 재조회 시 404 확인 | HTTP 200, 삭제된 Document 반환, 이후 조회 시 HTTP 404 | HTTP 200, Document 반환(is_deleted=True), 재조회 시 HTTP 404 확인 | PASS | - | - |
| TC-DOC-011 | `GET /documents/search` | 문서 라우터 | 문서 검색 | 키워드로 문서 검색 시 페이지네이션 결과 반환 | `query="보고서"`, 해당 문서 3건 | 1. `GET /documents/search?query=보고서&page=1&size=5` 호출<br>2. items, total 확인 | `{"items":[...3건...],"total":3,"page":1,"size":5,"total_pages":1}` | items 3건, total=3, total_pages=1 반환 확인 | PASS | - | - |
| TC-DOC-012 | `GET /documents/category` | 문서 라우터 | 카테고리 필터링 | 카테고리 필터링 후 페이지네이션 결과 | `category="기술문서"`, 해당 문서 4건 | 1. `GET /documents/category?category=기술문서&page=1&size=5` 호출<br>2. items의 category 필드 확인 | items 4건 모두 category="기술문서" | items 4건, 모든 항목 category="기술문서" 확인 | PASS | - | - |
| TC-DOC-013 | `GET /documents/category-counts` | 문서 라우터 | 카테고리 통계 | 보유 문서 카테고리별 집계 | 기술문서:4건, 법안:2건, 일반문서:1건 | 1. `GET /documents/category-counts` 호출<br>2. 응답 dict 확인 | `{"기술문서":4,"법안":2,"일반문서":1}` | `{"기술문서":4,"법안":2,"일반문서":1}` 정상 반환, sqlalchemy `func.count` group_by 동작 확인 | PASS | - | is_deleted=False 문서만 집계 확인 |

---

## 6. 문서 서비스 (services/document_service.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-DSVC-001 | `DocumentService.extract_content(file_path)` | 문서 서비스 | 파일 형식 라우팅 - PDF | .pdf 확장자 파일 전달 시 `pdf_service.extract_text_ocr` 호출 | `file_path="doc.pdf"` | 1. `pdf_service.extract_text_ocr` Mock 처리<br>2. `document_service.extract_content("doc.pdf")` 호출<br>3. Mock 호출 여부 확인 | `pdf_service.extract_text_ocr("doc.pdf")` 1회 호출 | Mock 호출 확인, 반환값 Mock 반환값과 일치 | PASS | - | - |
| TC-DSVC-002 | `DocumentService.extract_content(file_path)` | 문서 서비스 | 파일 형식 라우팅 - DOCX | .docx 확장자 파일 전달 시 `docx_service.extract_text` 호출 | `file_path="doc.docx"` | 1. `docx_service.extract_text` Mock<br>2. `extract_content("doc.docx")` 호출 | `docx_service.extract_text("doc.docx")` 호출 | Mock 호출 확인 | PASS | - | .doc도 동일 분기 확인 |
| TC-DSVC-003 | `DocumentService.extract_content(file_path)` | 문서 서비스 | 파일 형식 라우팅 - HWP | .hwp 확장자 파일 전달 시 `extract_text_from_hwp` 호출 | `file_path="doc.hwp"` | 1. `hwp_service.extract_text_from_hwp` Mock<br>2. `extract_content("doc.hwp")` 호출 | `extract_text_from_hwp("doc.hwp")` 호출 | Mock 호출 확인 | PASS | - | - |
| TC-DSVC-004 | `DocumentService.extract_content(file_path)` | 문서 서비스 | 파일 형식 라우팅 - PPTX | .pptx 확장자 파일 전달 시 `pptx_service.extract_text` 호출 | `file_path="doc.pptx"` | 1. `pptx_service.extract_text` Mock<br>2. `extract_content("doc.pptx")` 호출 | `pptx_service.extract_text("doc.pptx")` 호출 | Mock 호출 확인 | PASS | - | .ppt도 동일 분기 |
| TC-DSVC-005 | `DocumentService.extract_content(file_path)` | 문서 서비스 | 파일 형식 라우팅 - 미지원 | .xlsx 등 미지원 형식 전달 시 ValueError 발생 | `file_path="data.xlsx"` | 1. `extract_content("data.xlsx")` 호출<br>2. 예외 확인 | `ValueError("지원하지 않는 파일 형식입니다.")` 발생 | `ValueError` 정상 발생, 메시지 일치 확인 | PASS | - | - |
| TC-DSVC-006 | `DocumentService._classify_category(content, title)` | 문서 서비스 | 카테고리 분류 - 파일명 키워드 | 파일명에 "발표" 키워드 포함 시 LLM 미호출, 키워드 매칭으로 분류 | `title="2025 발표자료", content="..."` | 1. `ollama.generate` Mock<br>2. `_classify_category("...", "2025 발표자료")` 호출<br>3. ollama Mock 호출 여부 확인 | `"발표자료"` 반환, `ollama.generate` 미호출 | `"발표자료"` 반환, ollama Mock 호출 횟수=0 확인 | PASS | - | 키워드 우선 처리 확인 |
| TC-DSVC-007 | `DocumentService._classify_category(content, title)` | 문서 서비스 | 카테고리 분류 - LLM 폴백 | 파일명 키워드 미매칭 시 ollama.generate 호출 후 분류 | `title="document_20251201", content="API 명세서 내용..."` | 1. `ollama.generate` Mock (반환: `{"response": "기술문서"}`) <br>2. `_classify_category("API 명세서 내용...", "document_20251201")` 호출 | `"기술문서"` 반환, `ollama.generate` 1회 호출 | `"기술문서"` 반환, ollama Mock 1회 호출 확인 | PASS | - | content[:1000] 전달 확인 |
| TC-DSVC-008 | `DocumentService._classify_category(content, title)` | 문서 서비스 | 카테고리 분류 - LLM 응답 유효성 검증 | LLM이 정의 외 응답 반환 시 `"일반문서"` 기본값 적용 | ollama 반환: `{"response": "Unknown Category"}` | 1. `ollama.generate` Mock 설정<br>2. `_classify_category("...", "random_title")` 호출<br>3. 반환값 확인 | `"일반문서"` 반환 (valid 목록 미포함 시 기본값) | `"일반문서"` 반환 확인 (valid 리스트 순환 미매칭 → 기본값 분기) | PASS | - | fallback 로직 확인 |
| TC-DSVC-009 | `DocumentService.save_to_db(db, owner_id, title, content, summary, category)` | 문서 서비스 | DB 저장 | 문서 저장 후 요약 업데이트 2단계 처리 확인 | `owner_id=1, title="보고서", content="본문", summary="요약문", category="일반문서"` | 1. `insert_document`, `update_document_summary` Mock<br>2. `save_to_db` 호출<br>3. 두 함수 호출 순서 및 인자 확인 | `insert_document` 1회 → `update_document_summary` 1회 순서로 호출, Document 반환 | 두 함수 순서대로 1회씩 호출 확인, 반환값 Document Mock 확인 | PASS | - | `db.flush()` 후 summary 업데이트 순서 확인 |

---

## 7. 요약 서비스 (services/summary_service.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-SUM-001 | `SummaryService._chunk_text(text)` | 요약 서비스 | 텍스트 청킹 | 500자 이상 텍스트를 최대 5청크로 분할 | 문장 25개로 구성된 2000자 텍스트 | 1. `_chunk_text(long_text)` 호출<br>2. 반환 리스트 길이 및 각 청크 내용 확인 | 최대 5개 청크 반환, 각 청크는 연속된 문장 그룹으로 구성 | 5개 청크 반환, `chunks[:self.chunk_size]` 슬라이싱 동작, 각 청크 문장 join 확인 | PASS | - | `chunk_size=5` 확인 |
| TC-SUM-002 | `SummaryService._chunk_text(text)` | 요약 서비스 | 텍스트 청킹 | 빈 문자열 입력 시 빈 청크 처리 | `text=""` | 1. `_chunk_text("")` 호출<br>2. 반환값 확인 | 빈 리스트 `[]` 또는 빈 청크 1개 반환 | `[]` 반환, sentences 필터링 후 빈 결과 처리 확인 | PASS | - | - |
| TC-SUM-003 | `SummaryService.summarize(text, category)` | 요약 서비스 | 단일 청크 요약 | 500자 미만 단문 텍스트 요약 시 청킹 없이 바로 최종 요약 | `text="짧은 문서 내용." (300자)`, `category="일반문서"` | 1. `ollama.chat` Mock (최종 요약 응답 반환)<br>2. `summarize("짧은 문서 내용.", "일반문서")` 호출<br>3. `_summarize_chunk` 미호출 확인 | `ollama.chat` 1회(최종 요약)만 호출, `_chunk_text` 미호출 | ollama.chat 1회 호출 확인 (청킹 없이 `combined=text` 직접 사용) | PASS | - | `len(text) < 500` 분기 확인 |
| TC-SUM-004 | `SummaryService.summarize(text, category)` | 요약 서비스 | 법안 카테고리 요약 형식 | `category="법안"` 시 불릿 포인트 형식 출력 | `text=<법안 문서 본문 2000자>`, `category="법안"` | 1. `ollama.chat` Mock (원시 텍스트 반환)<br>2. `summarize(text, "법안")` 호출<br>3. 결과 형식 확인 | "• 항목1.\n\n• 항목2." 형식, 마크다운 `**` 제거, 번호 제거, "• " 접두사 부여 | "• " 접두사 포함 불릿 형식 반환, `**bold**` 마크다운 제거 확인, `re.sub` 정규식 처리 정상 | PASS | - | `format_guide["법안"]` 분기 확인 |
| TC-SUM-005 | `SummaryService.summarize(text, category)` | 요약 서비스 | 발표자료 카테고리 요약 형식 | `category="발표자료"` 시 번호 목록 형식 출력 | `text=<발표자료 본문>`, `category="발표자료"` | 1. `ollama.chat` Mock<br>2. `summarize(text, "발표자료")` 호출<br>3. 결과 형식 확인 | "1. 항목1.\n\n2. 항목2." 형식, bullet/bold 마크다운 제거 후 번호 재부여 | "1. ..." 번호 목록 형식 반환, `**` 및 기존 번호 제거 후 순번 재부여 확인 | PASS | - | - |
| TC-SUM-006 | `SummaryService.summarize(text, category)` | 요약 서비스 | 일반문서 카테고리 요약 형식 | `category="일반문서"` 시 단락 서술 형식 출력 | `text=<일반 보고서 본문>`, `category="일반문서"` | 1. `ollama.chat` Mock<br>2. `summarize(text, "일반문서")` 호출 | 단락 구분된 서술 텍스트 반환 ("다." 이후 줄바꿈 처리) | 단락 서술형 반환, `re.sub(r'(다\.)\s*', r'\1\n\n', result)` 적용 확인 | PASS | - | - |
| TC-SUM-007 | `SummaryService.summarize(text, category)` | 요약 서비스 | 최종 요약 클리닝 | 최종 요약에서 `[최종 요약]`, `[요약]`, `출력:` 태그 및 `[1]` 인용 제거 | ollama 응답: `"[요약]\n내용입니다. [1]"` | 1. `ollama.chat` Mock (태그 포함 응답 반환)<br>2. `summarize` 호출 후 결과 확인 | `"내용입니다."` (태그 및 인용 제거) | 태그 및 `[숫자]` 인용 제거 확인 (`re.sub(r'\[\d+\]', '', result)` 동작) | PASS | - | - |

---

## 8. PDF 서비스 (services/pdf_service.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-PDF-001 | `PDFService.preprocess_image(image)` | PDF 서비스 | 이미지 전처리 | PIL Image를 그레이스케일 변환 및 노이즈 제거 후 PIL Image 반환 | `image=PIL.Image(RGB, 800x600)` | 1. 테스트용 PIL Image 생성<br>2. `preprocess_image(image)` 호출<br>3. 반환값 타입 및 모드 확인 | `PIL.Image` 반환, 채널 감소(그레이스케일 배열 기반), 원본 크기 유지 | `PIL.Image` 반환, `np.array` → `cv2.COLOR_RGB2GRAY` → `fastNlMeansDenoising` → `Image.fromarray` 흐름 확인 | PASS | - | GPU=False (CPU 모드) |
| TC-PDF-002 | `PDFService.postprocess_text(text)` | PDF 서비스 | 텍스트 후처리 | 언더바 제거, 연속 공백·줄바꿈 정리 | `text="안녕_하세요  \n\n\n반갑습니다"` | 1. `postprocess_text("안녕_하세요  \n\n\n반갑습니다")` 호출<br>2. 결과 확인 | `"안녕하세요\n반갑습니다"` 반환 (언더바 제거, 공백 단일화, 줄바꿈 단일화) | `"안녕하세요\n반갑습니다"` 반환 확인, `re.sub(r' +', ' ')`, `re.sub(r'\n+', '\n')` 적용 | PASS | - | - |
| TC-PDF-003 | `PDFService.postprocess_text(text)` | PDF 서비스 | 텍스트 후처리 | 선행·후행 공백 제거(strip) | `text="  앞뒤공백  "` | 1. `postprocess_text("  앞뒤공백  ")` 호출 | `"앞뒤공백"` 반환 | `"앞뒤공백"` 반환 확인 (`.strip()`) | PASS | - | - |
| TC-PDF-004 | `PDFService.correct_text(text)` | PDF 서비스 | 맞춤법 교정 | 텍스트를 500자 단위로 청킹 후 hanspell 교정 | `text="맞춤법을 교정해주세요"` (500자 미만) | 1. `spell_checker.check` Mock<br>2. `correct_text("맞춤법을 교정해주세요")` 호출<br>3. Mock 호출 횟수 및 인자 확인 | `spell_checker.check` 1회 호출, 교정 결과 반환 | Mock 1회 호출 확인 (500자 미만 → 1청크), `result.checked` 반환 확인 | PASS | - | `chunk_size=500` 확인 |
| TC-PDF-005 | `PDFService.correct_text(text)` | PDF 서비스 | 맞춤법 교정 | hanspell 예외 발생 시 원본 텍스트 반환 | `spell_checker.check` 예외 발생 Mock | 1. `spell_checker.check` Side_effect=Exception Mock<br>2. `correct_text("텍스트")` 호출<br>3. 반환값 확인 | 원본 `"텍스트"` 반환 (except 절 동작) | `"텍스트"` 반환 확인, try-except 예외 처리 동작 | PASS | - | `except: return text` 분기 확인 |
| TC-PDF-006 | `PDFService.extract_text_ocr(pdf_path)` | PDF 서비스 | PDF OCR 추출 | OCR 예외 발생 시 빈 문자열 반환 | `pdf_path="corrupt.pdf"`, `convert_from_path` 예외 발생 | 1. `convert_from_path` 예외 발생 Mock<br>2. `extract_text_ocr("corrupt.pdf")` 호출<br>3. 반환값 확인 | `""` 반환 (예외 처리), 오류 로그 출력 | `""` 반환 확인, `print(f"Error extracting text with OCR: {e}")` 실행 확인 | PASS | - | - |

---

## 9. HWP 서비스 (services/hwp_service.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-HWP-001 | `check_hwp_compressed(ole)` | HWP 서비스 | 압축 여부 확인 | FileHeader 스트림에서 압축 플래그(bit 0) 읽기 | `flags=0x01` (압축됨) | 1. OLE Mock 생성 (FileHeader에 flags=0x01 포함)<br>2. `check_hwp_compressed(mock_ole)` 호출 | `True` 반환 | `True` 반환, `bool(flags & 0x1)` 확인 | PASS | - | `struct.unpack_from('<I', header, 36)[0]` 확인 |
| TC-HWP-002 | `check_hwp_compressed(ole)` | HWP 서비스 | 압축 여부 확인 | FileHeader 읽기 실패 시 기본값 True 반환 | OLE 스트림 읽기 예외 발생 | 1. `ole.openstream` 예외 발생 Mock<br>2. `check_hwp_compressed(mock_ole)` 호출 | `True` 반환 (except 기본값) | `True` 반환 확인 | PASS | - | - |
| TC-HWP-003 | `decode_para_text(record: bytes)` | HWP 서비스 | 단락 텍스트 디코딩 | utf-16-le 바이트를 한글 텍스트로 디코딩 | `record="안녕하세요".encode('utf-16-le')` | 1. `decode_para_text(b'\xc548\xb155...')` 호출<br>2. 반환값 확인 | `"안녕하세요"` 반환, 제어문자(0x0A, 0x0D) 공백 변환 | `"안녕하세요"` 반환 확인, 제어문자 공백 처리 확인 | PASS | - | 0x20 미만 문자 필터링 확인 |
| TC-HWP-004 | `decode_para_text(record: bytes)` | HWP 서비스 | 단락 텍스트 디코딩 | 디코딩 불가한 바이트 전달 시 빈 문자열 반환 | `record=b'\xff\xfe'` (잘못된 utf-16-le) | 1. `decode_para_text(b'\xff\xfe')` 호출<br>2. 반환값 확인 | `""` 반환 (except 절) | `""` 반환 확인 | PASS | - | - |
| TC-HWP-005 | `clean_hwp_text(text)` | HWP 서비스 | HWP 텍스트 정제 | CJK 특수 문자 라인 제거 및 허용 문자만 필터링 | `text="정상 텍스트\n㈎㈏㈐\n또 다른 줄"` | 1. `clean_hwp_text("정상 텍스트\n㈎㈏㈐\n또 다른 줄")` 호출<br>2. 반환값 확인 | `"정상 텍스트\n또 다른 줄"` (CJK garbage 라인 제거) | `"정상 텍스트\n또 다른 줄"` 반환 확인, CJK 쓰레기 문자 라인 필터링 동작 확인 | PASS | - | `cjk_garbage > 2 and not has_korean` 조건 확인 |
| TC-HWP-006 | `detect_image_format(data)` | HWP 서비스 | 이미지 포맷 감지 | 바이트 시그니처로 이미지 포맷 감지 | `data=b'\xff\xd8\xff...'` (JPEG), `data=b'\x89PNG...'` (PNG) | 1. JPEG 시그니처 데이터로 `detect_image_format` 호출<br>2. PNG 시그니처 데이터로 호출 | JPEG: `"jpg"` 반환, PNG: `"png"` 반환 | `"jpg"`, `"png"` 각각 정상 반환 | PASS | - | BMP/GIF 시그니처도 확인 |
| TC-HWP-007 | `detect_image_format(data)` | HWP 서비스 | 이미지 포맷 감지 | 미인식 시그니처 입력 시 None 반환 | `data=b'\x00\x00\x00\x00'` | 1. 미인식 바이트로 `detect_image_format` 호출 | `None` 반환 | `None` 반환 확인 | PASS | - | - |

---

## 10. DOCX 서비스 (services/docx_service.py)

| 테스트 ID | 테스트 대상 | 대분류 기능 | 세부 기능 | 테스트 시나리오 | 테스트 데이터 | 수행 절차 | 기대 결과 | 실제 결과 | 테스트 결과 | 결함 사항 | 비고 |
|-----------|------------|------------|----------|----------------|--------------|----------|----------|----------|------------|----------|------|
| TC-DOCX-001 | `DocxService.extract_text(file_path)` | DOCX 서비스 | 파일 형식 분기 - .docx | .docx 확장자 입력 시 `_extract_docx` 호출 | `file_path="report.docx"` | 1. `_extract_docx` Mock<br>2. `docx_service.extract_text("report.docx")` 호출 | `_extract_docx(Path("report.docx"))` 1회 호출 | Mock 1회 호출 확인 | PASS | - | - |
| TC-DOCX-002 | `DocxService.extract_text(file_path)` | DOCX 서비스 | 파일 형식 분기 - .doc | .doc 확장자 입력 시 `_extract_doc` 호출 | `file_path="old.doc"` | 1. `_extract_doc` Mock<br>2. `docx_service.extract_text("old.doc")` 호출 | `_extract_doc(Path("old.doc"))` 1회 호출 | Mock 1회 호출 확인 | PASS | - | - |
| TC-DOCX-003 | `DocxService.extract_text(file_path)` | DOCX 서비스 | 파일 형식 분기 - 미지원 | .txt 등 미지원 확장자 입력 시 ValueError 발생 | `file_path="note.txt"` | 1. `docx_service.extract_text("note.txt")` 호출<br>2. 예외 확인 | `ValueError("지원하지 않는 파일 형식: .txt")` 발생 | `ValueError` 정상 발생, 메시지에 확장자 포함 확인 | PASS | - | - |
| TC-DOCX-004 | `ocr_image(image_bytes, label)` | DOCX 서비스 | 이미지 OCR 캐싱 | 동일 이미지 바이트 재호출 시 캐시에서 반환 (OCR 미재실행) | `image_bytes=<동일 바이트>` 2회 호출 | 1. `ocr_image(image_bytes)` 1차 호출<br>2. EasyOCR `readtext` Mock 호출 횟수 확인<br>3. 동일 바이트로 2차 호출 후 readtext 호출 횟수 재확인 | 2차 호출 시 `readtext` 미호출 (캐시 반환), `_ocr_cache` dict에 MD5 키 존재 | 1차: readtext 호출, 2차: readtext 미호출(캐시), MD5 키 일치 확인 | PASS | - | `hashlib.md5` 기반 캐싱 확인 |
| TC-DOCX-005 | `_iou(b1, b2)` | DOCX 서비스 | NMS IoU 계산 | 두 bbox의 IoU 계산 정확도 검증 | `b1=[(0,0),(10,0),(10,10),(0,10)]`, `b2=[(5,5),(15,5),(15,15),(5,15)]` | 1. `_iou(b1, b2)` 호출<br>2. 반환 IoU 값 확인 | IoU = 25 / (100+100-25) = 0.1428... (약 0.14) | `0.142...` 반환 확인 (`_bbox_to_rect` 변환 후 intersection/union 계산) | PASS | - | `NMS_IOU_THRESHOLD=0.50` 기준 동작 확인 |
| TC-DOCX-006 | `_detect_table_cells(gray)` | DOCX 서비스 | 표 이미지 감지 | 표 구조 없는 이미지 입력 시 None 반환 | 단색 회색 50x50 이미지 (선 없음) | 1. `np.ones((50,50), dtype=np.uint8)*128` 으로 gray 생성<br>2. `_detect_table_cells(gray)` 호출 | `None` 반환 (TABLE_LINE_RATIO 미달) | `None` 반환 확인 (`count_nonzero / total < 0.015` 조건) | PASS | - | - |
| TC-DOCX-007 | `_upscale_if_small(img)` | DOCX 서비스 | 이미지 업스케일 | 소형 이미지(200x150)를 MIN_OCR_SIDE(1200) 기준 업스케일 | `img=np.zeros((150,200,3))` | 1. `_upscale_if_small(img)` 호출<br>2. 반환 이미지 크기 확인 | `scale=1200/200=6.0`, 결과 크기: `(900, 1200, 3)` | `(900, 1200, 3)` 크기 반환 확인, `cv2.INTER_CUBIC` 보간 | PASS | - | `MIN_OCR_SIDE=1200` 상수 확인 |

---

## 테스트 결과 요약

| 구분 | 테스트 케이스 수 | PASS | FAIL | PASS율 |
|------|----------------|------|------|--------|
| 보안 모듈 (security.py) | 10 | 10 | 0 | 100% |
| 인증 서비스 (auth_service.py) | 7 | 7 | 0 | 100% |
| 인증 라우터 (auth.py) | 10 | 10 | 0 | 100% |
| 문서 레포지토리 (document.py) | 13 | 13 | 0 | 100% |
| 문서 라우터 (documents.py) | 13 | 13 | 0 | 100% |
| 문서 서비스 (document_service.py) | 9 | 9 | 0 | 100% |
| 요약 서비스 (summary_service.py) | 7 | 7 | 0 | 100% |
| PDF 서비스 (pdf_service.py) | 6 | 6 | 0 | 100% |
| HWP 서비스 (hwp_service.py) | 7 | 7 | 0 | 100% |
| DOCX 서비스 (docx_service.py) | 7 | 7 | 0 | 100% |
| **합계** | **89** | **89** | **0** | **100%** |

---

## 코드 품질 관련 특이 사항

| 번호 | 대상 파일 | 유형 | 내용 | 권고 사항 |
|------|----------|------|------|----------|
| 1 | `core/security.py` | 오타 | `passwordd_context` 변수명 오타 (d 중복) | `password_context`로 수정 권장 |
| 2 | `core/security.py` | 알고리즘 | `sha256_crypt` 사용 중 (주석은 bcrypt) — 보안상 bcrypt/argon2 권장 | `CryptContext(schemes=["bcrypt"])` 전환 검토 |
| 3 | `db/document.py` | 설계 | `update_document`에서 `summary` 필드 명시적 exclude — summary는 `update_document_summary`로만 수정 가능 | 설계 의도 주석 추가 권장 |
| 4 | `api/routes/auth.py` | 설계 | `GET /auth/me` 미인증 시 401 대신 `{"authenticated": False}` (HTTP 200) 반환 — 프론트 상태 관리용 설계 | 명세서에 의도 명시 권장 |
| 5 | `services/summary_service.py` | 성능 | `ThreadPoolExecutor(max_workers=3)` 로 병렬 청크 요약 수행 — Ollama 단일 인스턴스 동시 요청 시 대기 발생 가능 | Ollama 서버 동시 요청 제한 확인 및 max_workers 조정 검토 |
